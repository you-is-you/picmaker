<% title '診断を作成する' %>
<div class="text-center">
    <div class="container">
        <div class="row">
            <div class="col-xs-12 col-md-12 mt-5">
            <h1><i class="fa fa-pencil-square-o" aria-hidden="true"></i>クイズ診断を作成する</h1>
            <%= link_to "名前診断に変更", '/tests/new', class:"btn btn-sm btn-primary", data: {"turbolinks" => false} %>
            <%= render 'form', quiz: @quiz %>
            </div>
        </div>
    </div>
</div>