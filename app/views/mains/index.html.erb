<% title '占いメーカー' %>
<% description 'ツイッターなどのSNSでシェアできるおもしろ診断で遊べるサイトです。人気の診断で遊べるだけでなく、オリジナルの診断を作ることもできますので、お気軽にお試しください。' %>
<div class="text-center">
    <div class="container">
        <div class="row">
            <div class="col-xs-12 col-md-12 mt-5">
                <div class="bg-warning p-3 mb-5 rounded">
                    <div class="border mx-auto bg-white">
                        <p class="p-3 h5 d-block">
                            診断を作って内輪でシェアして遊ぼう！
                        </p>
                        <% if session[:oauth_token].nil? %>
                            <%= link_to new_quiz_path, class:"btn btn-lg btn-primary m-3 d-inline-block", data: {"turbolinks" => false} do %>
                                <i class="fa fa-pencil-square-o d-block" aria-hidden="true">診断を作る</i>
                            <% end %>
                            <div class="d-block h5 p-3">ログインすれば、あなたの名前で診断作れます。</div>
                            <div class="d-block h5 p-3">ログインしなくても、診断を作って遊べます。</div>
                        <% else %>
                            <%= link_to new_quiz_path, class:"btn btn-lg btn-primary mb-3 d-inline-block", data: {"turbolinks" => false} do %>
                                <i class="fa fa-pencil-square-o" aria-hidden="true">診断を作る</i>
                            <% end %>
                            <div class="d-block h5 col-xs-4"><%= session[:name] %>のオリジナル診断をつくろう！</div>
                        <% end %>
                    </div>
                </div>
                <%= render 'ranking_test', test: @tests %>
                <%= render 'ranking_quiz', test: @quiz %>
            </div>
        </div>
    </div>
</div>